import type { IncomeRecord, ExpenseRecord } from '../types/finance';

// No sample data — application starts with a clean empty state.
export const INITIAL_INCOMES: IncomeRecord[] = [];
export const INITIAL_EXPENSES: ExpenseRecord[] = [];
